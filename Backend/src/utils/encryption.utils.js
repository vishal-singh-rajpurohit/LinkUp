const crypto = require("crypto")


const alog = "";

const hashData = (message, key) =>{
    try {
        
    } catch (error) {
        console.log("Error in hashData function")
    }
}

const createPrivateKey = () =>{}